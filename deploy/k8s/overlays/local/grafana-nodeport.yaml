# Exposes Grafana on the kind node port mapped to localhost:3000.
apiVersion: v1
kind: Service
metadata:
  name: grafana-external
  namespace: payment-processor
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
    - port: 3000
      nodePort: 30300
